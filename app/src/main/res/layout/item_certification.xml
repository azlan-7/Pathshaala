<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Certification Title -->
        <TextView
            android:id="@+id/tvCertificationTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Certification Name"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#0F4D73"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <!-- Issuing Organisation -->
        <TextView
            android:id="@+id/tvOrganisation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Issuing Organisation"
            android:textSize="16sp"
            android:textColor="#0F4D73"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvCertificationTitle"/>

        <!-- Year of Certification -->
        <TextView
            android:id="@+id/tvYear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Year"
            android:textSize="16sp"
            android:textColor="#0F4D73"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvOrganisation"/>

        <!-- Credential URL -->
        <TextView
            android:id="@+id/tvCredentialUrl"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Credential URL"
            android:textSize="16sp"
            android:textColor="#0F4D73"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvYear"/>

        <!-- Delete Button -->
        <ImageView
            android:id="@+id/btnDeleteCertification"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/delete_24dp_0f4d73_fill0_wght400_grad0_opsz24"
            android:contentDescription="Delete Certification"
            android:padding="4dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
