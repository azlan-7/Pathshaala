<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background Color -->
    <solid android:color="#1674AE" /> <!-- Purple Button (Change as needed) -->

    <!-- Border -->
  <stroke
       android:width="1dp"
      android:color="#212529" />

    <!-- Corner Radius -->
    <corners android:radius="20sp" />

    <!-- Padding (Optional) -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
